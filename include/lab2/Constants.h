// ============================================================================
// ФАЙЛ CONSTANTS.H - ОБЪЯВЛЕНИЕ ОБЩИХ КОНСТАНТ ПРОЕКТА LAB_2
// ============================================================================
// Назначение файла:
// 1) Объявить числовые и текстовые константы, которые используются в методе,
//    GUI, трассировке и вспомогательных проверках.
// 2) Собрать "магические числа" в одном месте, чтобы не дублировать их по
//    нескольким .cpp/.h файлам.
// 3) Дать единый источник значений по умолчанию для параметров метода
//    Розенброка с дискретным шагом.
// ============================================================================

// Защита от повторного включения заголовочного файла.
#pragma once

// std::size_t нужен для размерностей, лимитов и счетчиков.
#include <cstddef>

namespace lab2
{

// ============================================================================
// ОГРАНИЧЕНИЯ РАЗМЕРНОСТИ
// ============================================================================
// MAX_CUSTOM_DIMENSION:
// - максимальная размерность задачи, которую разрешено вводить пользователю.
// MAX_PARSER_DIMENSION:
// - максимальный индекс переменной, который допускается в тексте формулы
//   (например x1, x2, x3).
// ============================================================================
extern const std::size_t MAX_CUSTOM_DIMENSION;   // Максимальная размерность задачи, разрешенная в GUI.
extern const std::size_t MAX_PARSER_DIMENSION;   // Максимальный индекс переменной x_i, допустимый в формуле.

// ============================================================================
// МАТЕМАТИЧЕСКИЕ КОНСТАНТЫ
// ============================================================================
// Используются при регистрации констант "pi" и "e" в парсере формул.
// ============================================================================
extern const double MATH_PI_VALUE;               // Численное значение математической константы pi.
extern const double MATH_E_VALUE;                // Численное значение математической константы e.

// ============================================================================
// ШИРИНЫ СТОЛБЦОВ/ПОЛЕЙ ТРАССИРОВКИ
// ============================================================================
// Эти значения используются при текстовом форматировании строк трассы метода.
// Они задают базовые ширины колонок для индекса, номера итерации, векторов,
// чисел, шагов, откатов и информации о перестройке базиса.
// ============================================================================
extern const int TRACE_W_IDX;                    // Базовая ширина колонки с индексом строки трассы.
extern const int TRACE_W_K;                      // Базовая ширина колонки с номером внешней итерации k.
extern const int TRACE_W_J;                      // Базовая ширина колонки с номером шага j внутри итерации.
extern const int TRACE_W_VEC;                    // Базовая ширина колонки с вектором точки или направления.
extern const int TRACE_W_NUM;                    // Базовая ширина колонки с числовым значением функции.
extern const int TRACE_W_STEP;                   // Базовая ширина колонки с длиной шага Delta_j.
extern const int TRACE_W_ROLLBACK;               // Базовая ширина колонки с отметкой отката.
extern const int TRACE_W_DIR;                    // Базовая ширина колонки с признаком смены направления.
extern const int TRACE_W_REBUILD;                // Базовая ширина колонки с признаком перестройки базиса.
extern const int TRACE_W_BASIS;                  // Базовая ширина колонки с текстом нового базиса.

// ============================================================================
// ПАРАМЕТРЫ МЕТОДА ПО УМОЛЧАНИЮ
// ============================================================================
// Содержат значения по умолчанию для:
// - точности epsilon;
// - коэффициентов растяжения/сжатия alpha и beta;
// - начальной длины шага Delta_0;
// - специальной учебной точности для обязательных экспериментов;
// - предельного числа внешних итераций k.
// ============================================================================
extern const double DEFAULT_EPSILON;             // Основная точность epsilon по умолчанию для обычного запуска.
extern const double COARSE_EPSILON;              // Более грубая точность для быстрых демонстрационных сценариев.
extern const double DEFAULT_ALPHA;               // Коэффициент растяжения alpha по умолчанию (alpha > 1).
extern const double DEFAULT_BETA;                // Коэффициент сжатия beta по умолчанию (beta in (-1, 0)).
extern const double DEFAULT_INITIAL_STEP;        // Начальная длина шага Delta_j по умолчанию.
extern const double REQUIRED_EXPERIMENT_EPSILON; // Учебная точность, используемая в обязательных экспериментах.
extern const std::size_t DEFAULT_MAX_ITERATIONS; // Предохранительный лимит числа внешних итераций метода.

// ============================================================================
// ЧИСЛЕННЫЙ ДОПУСК ДЛЯ НАПРАВЛЕНИЙ
// ============================================================================
// Используется при проверках линейной независимости, ортогонализации и
// сравнении направляющих векторов базиса.
// ============================================================================
extern const double DIRECTION_TOLERANCE;         // Численный допуск при сравнении и ортогонализации направлений.

// ============================================================================
// ГОТОВЫЕ ТЕКСТЫ ФУНКЦИЙ И ПОДСКАЗОК ДЛЯ GUI
// ============================================================================
// Содержат:
// - стандартные выражения тестовых функций F1 и F2;
// - стартовые точки по умолчанию для GUI;
// - текст placeholder для поля ввода стартовой точки.
// ============================================================================
extern const char* FUNCTION_F1_EXPRESSION_TEXT;        // Текст стандартной двумерной функции F1.
extern const char* FUNCTION_F2_EXPRESSION_TEXT;        // Текст стандартной трехмерной функции F2.
extern const char* GUI_DEFAULT_START_POINT_F1_TEXT;    // Стартовая точка по умолчанию для сценариев с F1.
extern const char* GUI_DEFAULT_START_POINT_F2_TEXT;    // Стартовая точка по умолчанию для сценариев с F2.
extern const char* GUI_PLACEHOLDER_START_POINT_TEXT;   // Подсказка placeholder для поля ввода стартовой точки.

}  // namespace lab2
