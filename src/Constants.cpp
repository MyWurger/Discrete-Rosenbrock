// ============================================================================
// ФАЙЛ CONSTANTS.CPP - ОПРЕДЕЛЕНИЕ ОБЩИХ КОНСТАНТ ПРОЕКТА LAB_2
// ============================================================================
// Здесь задаются конкретные значения констант, объявленных в Constants.h.
// Эти значения используются в:
// - методе Розенброка;
// - GUI;
// - текстовой трассировке;
// - стандартных учебных сценариях.
// ============================================================================

// Подключаем объявления всех общих констант проекта.
#include "lab2/Constants.h"

namespace lab2
{

// ============================================================================
// ОГРАНИЧЕНИЯ РАЗМЕРНОСТИ
// ============================================================================
const std::size_t MAX_CUSTOM_DIMENSION = 3;   // Разрешаем пользователю работать не более чем с 3 переменными.
const std::size_t MAX_PARSER_DIMENSION = 3;   // Парсер также ограничиваем пространством размерности до 3.

// ============================================================================
// МАТЕМАТИЧЕСКИЕ КОНСТАНТЫ
// ============================================================================
const double MATH_PI_VALUE = 3.14159265358979323846;   // Число pi для выражений вида sin(pi/2).
const double MATH_E_VALUE = 2.71828182845904523536;    // Число e для выражений вида e^x.

// ============================================================================
// ШИРИНЫ СТОЛБЦОВ/ПОЛЕЙ ТРАССИРОВКИ
// ============================================================================
const int TRACE_W_IDX = 4;          // Ширина колонки с индексом строки трассы.
const int TRACE_W_K = 4;            // Ширина колонки с номером внешней итерации k.
const int TRACE_W_J = 4;            // Ширина колонки с номером шага j.
const int TRACE_W_VEC = 24;         // Ширина колонки с векторами точек и направлений.
const int TRACE_W_NUM = 12;         // Ширина колонки с числовыми значениями функции.
const int TRACE_W_STEP = 14;        // Ширина колонки с текущим Delta_j.
const int TRACE_W_ROLLBACK = 8;     // Ширина колонки с отметкой отката.
const int TRACE_W_DIR = 18;         // Ширина колонки с информацией о смене направления.
const int TRACE_W_REBUILD = 24;     // Ширина колонки с признаком перестройки базиса.
const int TRACE_W_BASIS = 70;       // Ширина колонки со строковым описанием нового базиса.

// ============================================================================
// ПАРАМЕТРЫ МЕТОДА ПО УМОЛЧАНИЮ
// ============================================================================
const double DEFAULT_EPSILON = 1e-3;                // Основная точность epsilon по умолчанию.
const double COARSE_EPSILON = 1e-1;                 // Грубая точность для быстрых демонстрационных запусков.
const double DEFAULT_ALPHA = 1.5;                   // Стандартный коэффициент растяжения alpha.
const double DEFAULT_BETA = -0.5;                   // Стандартный коэффициент сжатия beta.
const double DEFAULT_INITIAL_STEP = 0.1;            // Базовая начальная длина шага Delta_0.
const double REQUIRED_EXPERIMENT_EPSILON = 1e-2;    // Учебная точность для обязательных сравнительных экспериментов.
const std::size_t DEFAULT_MAX_ITERATIONS = 50000;   // Предохранительный лимит числа внешних итераций.

// ============================================================================
// ЧИСЛЕННЫЕ ДОПУСКИ
// ============================================================================
const double DIRECTION_TOLERANCE = 1e-12;   // Допуск для сравнения и ортогонализации направляющих векторов.

// ============================================================================
// ГОТОВЫЕ ТЕКСТЫ ФУНКЦИЙ И ПОДСКАЗОК ДЛЯ GUI
// ============================================================================
const char* FUNCTION_F1_EXPRESSION_TEXT = "9*x1^2 + 16*x2^2 - 90*x1 - 128*x2";   // Стандартное выражение функции F1.
const char* FUNCTION_F2_EXPRESSION_TEXT =
    "x1^2 + 2*x1*x2 + 2*x2^2 + x3^2 - x2*x3 + x1 + 3*x2 - x3";                    // Стандартное выражение функции F2.
const char* GUI_DEFAULT_START_POINT_F1_TEXT = "7 -2";                             // Стартовая точка F1 по умолчанию в GUI.
const char* GUI_DEFAULT_START_POINT_F2_TEXT = "0 0 0";                            // Стартовая точка F2 по умолчанию в GUI.
const char* GUI_PLACEHOLDER_START_POINT_TEXT = "0 0 0";                           // Текст-подсказка для поля ввода стартовой точки.

}  // namespace lab2
